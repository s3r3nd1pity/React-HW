import PaginationComponent from "../pagination/PaginationComponent.tsx";

const MenuComponent = () => {
    return (
        <div>
            <PaginationComponent/>
        </div>
    );
};

export default MenuComponent;